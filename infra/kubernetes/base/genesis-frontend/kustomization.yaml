apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - serviceaccount.yaml

commonLabels:
  app.kubernetes.io/name: genesis-frontend
  app.kubernetes.io/component: frontend
  app.kubernetes.io/part-of: genesis-protocol
