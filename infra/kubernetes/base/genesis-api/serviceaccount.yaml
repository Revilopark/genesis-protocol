apiVersion: v1
kind: ServiceAccount
metadata:
  name: genesis-api
  labels:
    app: genesis-api
  annotations:
    # For AWS IRSA (IAM Roles for Service Accounts)
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/genesis-api-role
